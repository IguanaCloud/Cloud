# AWS directory have scripts to deploy infrastructure in aws Cloud
## to navigate in directory 
```bash
cd aws
```
# GCP directory have scripts to deplot infrastructure in gcp CLoud
## to navigate in directory 
```bash
cd gcp
```